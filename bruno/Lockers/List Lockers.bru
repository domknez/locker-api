meta {
  name: List Lockers
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/lockers?limit=20&offset=0
}

params:query {
  limit: 20
  offset: 0
}

assert {
  res.status: eq 200
  res.body: isArray
}
